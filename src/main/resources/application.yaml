spring:
  application:
    name: Chat4Me - Auth Service
  datasource:
    url: jdbc:oracle:thin:@localhost:1521:orcl
    username: Loris_Chat4Me_MessagingService
    password: Password98
    driver-class-name: oracle.jdbc.OracleDriver
  jpa:
    database-platform: org.hibernate.dialect.OracleDialect
    hibernate:
      ddl-auto: update
    show-sql: true